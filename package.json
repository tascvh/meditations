{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deployx" : "NUXT_APP_BASE_URL=/meditations/ npx nuxt build --preset github_pages ;  npx gh-pages --dotfiles -d .output/public" ,
    "deploy" : "npx nuxt build --preset github_pages ;  npx gh-pages --dotfiles -d .output/public" ,
    "serve" : "nuxt generate ; serve .output/public"
  },
  "dependencies": {
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  }
}
